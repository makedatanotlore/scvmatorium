import names from 'rng/tables/names';
import equipment from 'rng/tables/equipment';
import traits from 'rng/tables/traits';
import habits from 'rng/tables/habits';
import bodies from 'rng/tables/bodies';

export default {
  names,
  equipment,
  traits,
  habits,
  bodies,
};
