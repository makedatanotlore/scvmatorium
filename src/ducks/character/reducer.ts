import { Action } from 'types/ducks';
const initialState = {};

export const reducer = (state: any = initialState, action: Action) => {
  switch (action.type) {
    default:
      return state;
  }
};
